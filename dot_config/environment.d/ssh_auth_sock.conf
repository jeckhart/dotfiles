# Make the 1Password agent bridge socket (ssh-agent-bridge.service) visible to
# the systemd *user manager* and every service it starts (e.g. radicle-node).
# Shells don't read environment.d — wsl2.zsh exports the same path for them.
SSH_AUTH_SOCK=$XDG_RUNTIME_DIR/ssh-agent.sock
